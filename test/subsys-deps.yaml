consumes:
  acme:
    admin:
      url: env
      password: path
      # env: true # ARKCASE_${SUBSYS}_${CONN}_${TYPE}_${PROPERTY}
  content:
    main:
      url: URL_FOR_FILEZ
      ui: DA_PURTY_UI
      username: MAH_ACCEZ_PAZZ
      password: /some/other/path/for/mounting
    main-admin:
      url: URL_FOR_FILEZ
      ui: DA_PURTY_UI
      username: /some/path/I/want/this/mounted/on
      password: GIB_ADMIN_PASSWERD_PLOX
