apiVersion: v2
name: pvc-rsync
version: 1.0.1
appVersion: latest
description: A Pod that mounts all the PVCs available in a given namespace, and exposes them over RSYNC
home: https://github.com/ArkCase/ark_helm_charts/tree/main/src/pvc-rsync
keywords:
- rsync
- pvc
maintainers:
- email: diego.rivera@armedia.com
  name: diego.rivera
type: application
dependencies:
  - name: common
    version: ~0.5.0
    repository: "https://arkcase.github.io/ark_helm_charts"
