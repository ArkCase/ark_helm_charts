################################################################################
################################################################################
################################################################################
################################################################################
################################################################################
################################################################################

{{- $edition := ((eq "true" (include "arkcase.enterprise" $)) | ternary "enterprise" "community") -}}
{{- $repository := "manually-given-repository" -}}
{{- $data := dict "global" ($.Values.global | default dict) "local" ($.Values.image | default dict) }}

-----
EDITION: [{{ $edition }}]
-----

{{- range $i := list "alpha" "bravo" -}}
################################################################################

{{ $i }}:
  image: {{- include "arkcase.image" (dict "ctx" $ "name" $i) | quote }}
  {{- include "arkcase.image.pullPolicy" (dict "ctx" $ "name" $i) | nindent 2 }}
  {{- include "arkcase.image.pullSecrets" (dict "ctx" $ "name" $i) | nindent 2 }}

################################################################################
{{- end -}}
