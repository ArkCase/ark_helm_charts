{{- if and (include "arkcase.subsystem.enabled" $) -}}
  {{- $contentDialect := (include "arkcase.content.dialect" $) -}}
  {{- if (eq "alfresco" $contentDialect) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "arkcase.fullname" $ | quote }}
  namespace: {{ $.Release.Namespace | quote }}
  labels: {{- include "arkcase.labels" $ | nindent 4 }}
    {{- with (.Values.labels).common }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- with (.Values.annotations).common }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
type: Opaque
stringData:
  # We can't modify these unless we also rotate out the keystore and change its
  # passwords WITHOUT modifying the key itself ... this is a pain, and we're not
  # yet ready for it.
  METADATA_KEYSTORE_PASSWORD: "mp6yc0UD9e"
  METADATA_KEYSTORE_METADATA_PASSWORD: "oKIWzVdEdA"
  {{- end -}}
{{- end -}}
