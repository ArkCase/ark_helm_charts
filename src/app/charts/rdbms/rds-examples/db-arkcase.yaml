apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Database
metadata:
  name: arkcase-rdbms-arkcase
  namespace: rds-test
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: User
metadata:
  name: arkcase-rdbms-arkcase
  namespace: rds-test
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
  writeConnectionSecretToRef:
    name: arkcase-rdbms-cred-schema-arkcase
    namespace: rds-test
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Grant
metadata:
  name: arkcase-rdbms-arkcase
  namespace: rds-test
spec:
  forProvider:
    privileges:
      - ALL
    userRef:
      name: arkcase-rdbms-arkcase
    databaseRef:
      name: arkcase-rdbms-arkcase
  providerConfigRef:
    name: arkcase-rdbms
