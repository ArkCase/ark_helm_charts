apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Database
metadata:
  name: arkcase-rdbms-pentaho-quartz
  namespace: rds-test
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: User
metadata:
  name: arkcase-rdbms-pentaho-quartz
  namespace: rds-test
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
  writeConnectionSecretToRef:
    name: arkcase-rdbms-cred-schema-pentaho-quartz
    namespace: rds-test
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Grant
metadata:
  name: arkcase-rdbms-pentaho-quartz
  namespace: rds-test
spec:
  forProvider:
    privileges:
      - ALL
    userRef:
      name: arkcase-rdbms-pentaho-quartz
    databaseRef:
      name: arkcase-rdbms-pentaho-quartz
  providerConfigRef:
    name: arkcase-rdbms
