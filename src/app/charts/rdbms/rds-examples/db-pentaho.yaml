apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Database
metadata:
  name: arkcase-rdbms-pentaho
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: User
metadata:
  name: arkcase-rdbms-pentaho
spec:
  forProvider: {}
  providerConfigRef:
    name: arkcase-rdbms
  writeConnectionSecretToRef:
    name: arkcase-rdbms-cred-schema-pentaho
---
apiVersion: mysql.sql.crossplane.io/v1alpha1
kind: Grant
metadata:
  name: arkcase-rdbms-pentaho
spec:
  forProvider:
    privileges:
      - ALL
    userRef:
      name: arkcase-rdbms-pentaho
    databaseRef:
      name: arkcase-rdbms-pentaho
  providerConfigRef:
    name: arkcase-rdbms
