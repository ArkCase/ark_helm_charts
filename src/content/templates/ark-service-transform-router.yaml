{{- if and (include "arkcase.enterprise" $) (not (include "arkcase.content.external" $)) (eq "alfresco" (include "arkcase.content.engine" $)) -}}
  {{- include "arkcase.subsystem.service" $ -}}
{{- end -}}
