{{- if and (or .Values.enabled (not (hasKey .Values "enabled"))) (include "common.persistence.isEnabled" .) }}

{{ include "common.persistence.validateVolumeConfig" ( dict "vol" ((.Values.persistence).data) "ctx" $ "name" "data" ) }}
{{- if not (((.Values.persistence).data).claim).name }}
{{- if not (((.Values.persistence).data).claim).spec }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "common.fullname" . }}-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).data).annotations) }}
{{- with .Values.persistence.data.annotations  }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
{{- end }}
{{- if not (((.Values.persistence).data).volume) }}
spec:
  storageClassName: "manual"
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - "ReadWriteOnce"
  capacity:
    storage: "1Gi"
  hostPath:
    path: "{{ coalesce (.Values.persistence).localPath ((.Values.global).persistence).localPath "/opt/app/arkcase" }}/data/samba"
{{- else }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).data).volume) "context" $) | nindent 2 }}
{{- end }}
{{- end }}

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "common.fullname" . }}-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).data).claim).spec }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).data).claim.spec) "context" $) | nindent 2 }}
{{- else }}
spec:
  storageClassName: "manual"
  volumeName: {{ template "common.fullname" . }}-data
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
{{- end }}
{{- end }}

{{ include "common.persistence.validateVolumeConfig" ( dict "vol" ((.Values.persistence).conf) "ctx" $ "name" "conf" ) }}
{{- if not (((.Values.persistence).conf).claim).name }}
{{- if not (((.Values.persistence).conf).claim).spec }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "common.fullname" . }}-conf
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).conf).annotations) }}
{{- with .Values.persistence.conf.annotations  }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
{{- end }}
{{- if not (((.Values.persistence).conf).volume) }}
spec:
  storageClassName: "manual"
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - "ReadWriteOnce"
  capacity:
    storage: "1Gi"
  hostPath:
    path: "{{ coalesce (.Values.persistence).localPath ((.Values.global).persistence).localPath "/opt/app/arkcase" }}/conf/samba"
{{- else }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).conf).volume) "context" $) | nindent 2 }}
{{- end }}
{{- end }}

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "common.fullname" . }}-conf
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).conf).claim).spec }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).conf).claim.spec) "context" $) | nindent 2 }}
{{- else }}
spec:
  storageClassName: "manual"
  volumeName: {{ template "common.fullname" . }}-conf
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
{{- end }}
{{- end }}

{{ include "common.persistence.validateVolumeConfig" ( dict "vol" ((.Values.persistence).logs) "ctx" $ "name" "logs" ) }}
{{- if not (((.Values.persistence).logs).claim).name }}
{{- if not (((.Values.persistence).logs).claim).spec }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "common.fullname" . }}-logs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).logs).annotations) }}
{{- with .Values.persistence.logs.annotations  }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
{{- end }}
{{- if not (((.Values.persistence).logs).volume) }}
spec:
  storageClassName: "manual"
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - "ReadWriteOnce"
  capacity:
    storage: "1Gi"
  hostPath:
    path: "{{ coalesce (.Values.persistence).localPath ((.Values.global).persistence).localPath "/opt/app/arkcase" }}/logs/samba"
{{- else }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).logs).volume) "context" $) | nindent 2 }}
{{- end }}
{{- end }}

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "common.fullname" . }}-logs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
{{- if (((.Values.persistence).logs).claim).spec }}
spec: {{- include "common.tplvalues.render" (dict "value" (((.Values.persistence).logs).claim.spec) "context" $) | nindent 2 }}
{{- else }}
spec:
  storageClassName: "manual"
  volumeName: {{ template "common.fullname" . }}-logs
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
{{- end }}
{{- end }}

{{- end }}
