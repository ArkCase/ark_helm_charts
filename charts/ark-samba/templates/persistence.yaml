{{- if (and (include "arkcase.tools.enabled" .) (include "arkcase.persistence.enabled" .)) }}

{{ include "arkcase.persistence.declareVolume" (dict "ctx" $ "name" "data") | nindent 0 }}
{{ include "arkcase.persistence.declareVolume" (dict "ctx" $ "name" "conf") | nindent 0 }}
{{ include "arkcase.persistence.declareVolume" (dict "ctx" $ "name" "logs") | nindent 0 }}

{{- end -}}
