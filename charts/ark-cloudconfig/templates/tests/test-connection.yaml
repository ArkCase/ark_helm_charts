apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ark-cloudconfig.fullname" . }}-test-connection"
  labels:
    {{- include "ark-cloudconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ark-cloudconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
