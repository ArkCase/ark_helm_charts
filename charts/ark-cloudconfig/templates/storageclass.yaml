kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: {{ .Values.arkcaseExtention.persistence.storageClassName | quote | default "efs-sc"}}
  namespace: {{ .Release.Namespace }}
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: {{ required "A valid EFS fsHandler requied " .Values.arkcaseExtention.persistence.fsHandlerId }}
  directoryPerms: "700"
  reclaimPolicy: RETAIN