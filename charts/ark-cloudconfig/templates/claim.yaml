apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ required "A valid claim name required to provision  pvc" .Values.arkcaseExtention.persistence.claimName }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    {{- range .Values.arkcaseExtention.persistence.accessModes }}
      - {{ . | quote }}
    {{- end }}
  storageClassName: {{ .Values.arkcaseExtention.persistence.storageClassName | quote | default "efs-sc"}}
  resources:
    requests:
      storage: {{ .Values.arkcaseExtention.persistence.size}}