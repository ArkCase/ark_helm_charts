application:
 name:
  active: arkcase,arkcase-activemq,arkcase-oidc,ldap,lookups
  default: arkcase
 profile: k8s,runtime
configuration:
 client:
  branding.path: "branding"
  labels.path: "labels"
  ldap.path: "ldap"
  lookups.path: "lookups"
  stylesheets.path: "pdf-stylesheets"
  rules.path: "rules"
  extraFields.path: "schemas/json"
  spring.path: "spring"
 server:
  url: {{ (((.Values).configuration).configServer).url | default "http://localhost:9999" | quote }}
{{- if (((.Values).configuration).configServer).username }}
  username: {{ (((.Values).configuration).configServer).username | quote }}
{{- end }}
{{- if (((.Values).configuration).configServer).password }}
  password: {{ (((.Values).configuration).configServer).password | quote }}
{{- end }}
  update.path: "/config/{applicationName}"
  remove.path: "/config/remove/{applicationName}"
  reset.path: "/reset"
  update.file.path: "/file"
  modules.path: "/config/modules"
