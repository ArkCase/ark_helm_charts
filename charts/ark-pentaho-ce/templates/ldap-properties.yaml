apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pentaho-ce.fullname" . }}-ldap-configmap
  labels: {{- include "ark-pentaho.selectorLabels" . | nindent 4 }}
  annotations:
    timestamp: "{{ date "20060102150405" now }}"
  namespace: {{ .Release.Namespace | quote }}
data:
    applicationContext-security-ldap.properties: |
      {{ .Values.ldap.externalHost.config | nindent 8 }}